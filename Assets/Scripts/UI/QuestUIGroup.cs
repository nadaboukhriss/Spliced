using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestUIGroup : MonoBehaviour
{
    /*private Dictionary<QuestItem, QuestUI> questUIDict;
    // Start is called before the first frame update
    void Start()
    {
        QuestUI[] questUIs = GetComponentsInChildren<QuestUI>();
        foreach(QuestUI ui in questUIs)
        {
            questUIDict[ui.questItem] = ui;
        }
    }

    public void CompleteQuest(QuestItem key)
    {
        questUIDict[key].Complete();
    }*/
}
